/**
 * Re-export shim — `selectTopAlignmentChips` + `AlignmentChipRow` now live in
 * `@chiaro/officials/derivations`. This file kept for backwards compat with
 * existing imports (`@/lib/derivations/alignment`); new consumers should import
 * from `@chiaro/officials` directly.
 */
export { selectTopAlignmentChips, type AlignmentChipRow } from '@chiaro/officials'
