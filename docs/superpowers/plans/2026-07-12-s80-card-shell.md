# S80 plan — card-shell unification

Spec: `docs/superpowers/specs/2026-07-12-s80-card-shell-design.md`.
Implementer subagents run SEQUENTIALLY (Gotcha #25).

- **T1 — primitives**: `cards/DetailCardShell.tsx` + `cards/EventRowList.tsx` (+
  hoisted `FORMAT_LABEL`) + barrel exports + shell/list unit tests (branch precedence,
  error-vs-empty, retry, h2 level, bg scheme, B6 null-url guard, meta null-skip).
- **T2 — federal migration**: 6 Federal*Card files render via the shell; town-halls +
  district-offices lists adopt EventRowList; per-card error wiring (isError/onRetry
  from the card's hooks); tests updated; officials-ui suite green.
- **T3 — state migration**: 6 State*Card files ditto (bg scheme change lands here);
  state town-halls/offices lists adopt EventRowList; tests updated; suite green.
- **T4 — Sentry rider**: global-error.tsx (+ render test), instrumentation-client.ts
  migration, disableLogger replacement, BrandTextInput autoComplete cast fix.
- **T5 — CLS**: local prod-build Lighthouse repro on /sign-in, diagnose the outer-
  container shift, fix, re-measure (<0.1 locally), record numbers.
- **T6 — battery + ship**: typecheck, lint, full pnpm test, build, CLAUDE.md entry,
  PR with before/after (shell grep counts + CLS), merge on green checks table,
  memory update.
