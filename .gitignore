# deps
node_modules/
.pnpm-store/

# build
dist/
.next/
.turbo/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
!.env.example

# supabase local artifacts
**/supabase/.branches/
**/supabase/.temp/
**/supabase/seed.sql.bak

# stray root supabase/ dir created if `supabase start` is run from repo root
# (the real config lives in packages/db/supabase/ — always run from there)
/supabase/

# tooling temp files
/_tmp_*

# expo
.expo/
.expo-shared/

# IDE
.vscode/
.idea/

# Claude Code worktrees (per-machine, not shared)
.claude/worktrees/

# Claude Code per-user settings + runtime artifacts (not shared)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# OpenStates v3 fetcher on-disk cache (per-machine, runtime artifact)
packages/db/supabase/seed/.cache/

# Superpowers brainstorm artifacts (per-machine, not shared)
.superpowers/

# system
.DS_Store
Thumbs.db

# deno.lock is generated by local `deno test` runs (slice 69 verification);
# Edge Function deploys and CI resolve from import specifiers, not a lockfile.
packages/db/**/deno.lock
